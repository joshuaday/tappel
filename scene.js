(function () {
	// determines how a set of images gets laid out and how events propagate into them
	
	// basic ideas:
	// 1. animations + offsets
	// 2. multiple scenes at once?
	// 3. map viewer / image compositor for preview (arbitrary)
})();

